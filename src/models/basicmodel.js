/*const mongoose = require('mongoose');

const habitacionShema = new mongoose.Schema({
    color: {
        type: String,
        required: true,
        trim: true
    },
    metroscuadrados: {
        type: Number,
        required: true,
    },
    ventanas: {
        type: Number,
        required: true,
    },
})


const Habitacion = mongoose.model('Habitacion', habitacionShema);
module.exports = Habitacion;*/